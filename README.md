启动命令(根目录启动，main.py所在目录启动,也可以直接运行python run.py就能启动)：

忽略已安装的包：
pip install -r requirements.txt --ignore-installed

包是python的一种组织，将多个模块组合在一起，形成一个大的python工具库，包通常是一个拥有__init__.py文件的目录，它定义了包的属性和方法

api文档链接：http://0.0.0.0:8000/docs
